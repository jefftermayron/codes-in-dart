void main() {
  double nota = 4;
  String mensagem = nota >= 6 ? 'Aprovado' : 'Reprovado' ;
  // int numero = nota == 5 ? 10 : 2;
  // como um if/else dentro de uma variável (exemplo)
  print (mensagem);

}
